name: documents_repository
description: A repository that exposes news data.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.3
  flutter_sdeng_api:
    path: ../../api
  path_provider: ^2.1.3
  pdf: ^3.10.8

dev_dependencies:
  coverage: ^1.2.0
  mocktail: ^1.0.2
  test: ^1.21.4
  very_good_analysis: ^5.1.0
